---
interface Props {
  label: string;
  variant?: "fill" | "outline";
  color?: "blue" | "purple" | "amber" | "green" | "red" | "default";
}

const fillMap: Record<string, string> = {
  blue: "bg-blue-500 text-white",
  purple: "bg-purple-500 text-white",
  amber: "bg-amber-500 text-black",
  green: "bg-emerald-500 text-white",
  red: "bg-red-500 text-white",
  default: "bg-text-tertiary text-white",
};

const outlineMap: Record<string, string> = {
  blue: "border-blue-500/40 text-blue-400",
  purple: "border-purple-500/40 text-purple-400",
  amber: "border-amber-500/40 text-amber-400",
  green: "border-emerald-500/40 text-emerald-400",
  red: "border-red-500/40 text-red-400",
  default: "border-text-tertiary text-text-tertiary",
};

const { label, variant = "fill", color = "default" } = Astro.props;
const styleClass = variant === "fill" ? fillMap[color] : outlineMap[color];
---

<span class={`inline-block rounded-md px-2.5 py-0.5 text-xs font-semibold ${styleClass} ${variant === "outline" ? "border" : ""}`}>
  {label}
</span>
